# Model provider credentials are optional until a concrete provider is configured.
OPENAI_API_KEY=

# Keep benchmark data outside git unless a task explicitly pins a tiny fixture.
CHATSQL_DATA_DIR=data
CHATSQL_RUNS_DIR=runs
